import React from 'react';

// Este componente ha sido reemplazado por DeudasCliente
// Se mantiene solo para compatibilidad con la ruta
export const Evaluacion = () => {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h2>Este componente ha sido reemplazado</h2>
      <p>Por favor utiliza el módulo de PRESTAMOS desde la página de inicio.</p>
    </div>
  );
};